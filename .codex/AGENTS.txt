你正在修改 Unity C# 项目中的 BuffSystem。当前项目已经有新版 ECS BuffSystem，它是从旧版 BuffSystem 融合进 ECS 后生成的，但存在功能回归和性能问题。

请严格遵守以下规则：

1. 优先阅读并修改 BuffSystem 相关文件，不要全项目大范围重构。
2. 只有在编译依赖确实需要时，才扩大阅读范围。
3. 不要重写整个系统，不要格式化无关文件，不要修改 .meta 文件，除非 Unity 资源引用必须变更。
4. 保留现有命名空间、现有公开 API 和现有数据结构的兼容性，除非任务明确要求新增接口。
5. 新增和修改的注释全部使用中文，包括 XML summary、字段注释、复杂逻辑注释。
6. Runtime 逻辑不得重新依赖 GameObject、Transform、MonoBehaviour、Time.time、Time.deltaTime。
7. Runtime 逻辑不得依赖 ScriptableObject 实例状态。ScriptableObject 只允许作为编辑器配置和数据源。
8. Buff 逻辑必须基于 ECS Entity、World、Component、SimulationContext。
9. 必须保持回滚友好：事件、Buff 添加、Buff 移除、Tick 都必须可按固定逻辑帧重放。
10. 性能优化不能通过删除功能实现。尤其不能删除事件响应、Tick、StackChanged、Remove 等旧版已有语义。
11. 不要在热路径中使用反射、LINQ、闭包分配、object 装箱。注册阶段或编辑器阶段可以使用反射。
12. 完成后输出：
    - 修改了哪些文件
    - 每个文件为什么修改
    - 是否存在兼容性风险
    - 是否需要我在 Unity 里手动验证